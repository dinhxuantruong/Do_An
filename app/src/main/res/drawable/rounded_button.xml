<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">  <!-- Định nghĩa hình dạng là hình chữ nhật -->
    <corners android:radius="10dp" />  <!-- Bo góc 16dp -->
    <solid android:color="@color/colorPrimary" />  <!-- Màu nền của button -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />  <!-- Padding cho button -->
</shape>
