<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/asc"
        android:title="Tăng dần"
      app:showAsAction="withText"
        android:icon="@drawable/t"/>

    <item android:id="@+id/desc"
        android:title="Giảm dần"
        app:showAsAction="withText"
        android:icon="@drawable/g"/>

    <item android:id="@+id/defaultt"
        android:title="Mặc định"
        app:showAsAction="withText"
        android:icon="@drawable/d_ault"/>
</menu>